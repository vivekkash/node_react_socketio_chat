{
  "name": "gups-chat-app",
  "version": "1.0.0",
  "description": "gups is a chatroom application",
  "main": "index.js",
  "scripts": {
    "build": "cd server && npm install && cd ../client && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "viveksagar kashyap",
  "license": "ISC"
}
